import { AutosizeTextareaDirective } from './autosize-textarea.directive';

describe('AutosizeTextareaDirective', () => {
  it('should create an instance', () => {
    const directive = new AutosizeTextareaDirective();
    expect(directive).toBeTruthy();
  });
});
